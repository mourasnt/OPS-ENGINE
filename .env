# Copy this file to populate environment variables for local development
# DO NOT commit real secrets to version control. Use .env only for local testing.

# Copy to .env and set production values (do NOT commit .env with real secrets)
RPA_USUARIO=35036755820
RPA_SENHA=120487@Ka
# Path inside container or host path to the Google service account JSON
GOOGLE_APPLICATION_CREDENTIALS=/dados/credentials.json

# Redis settings (optional; if your config.json uses them, you can override here)
REDIS_HOST=ops-engine-redis
REDIS_PORT=6379